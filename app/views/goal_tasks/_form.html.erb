<%= simple_form_for(@goal_task) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :goal %>
    <%= f.association :group %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
